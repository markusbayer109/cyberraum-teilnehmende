# Öffentliches Multi-Arch-Image: markusbayer109/cyberraum-cai:2026-09.
# Der Manifest-Digest hält den veröffentlichten Stand für amd64 und arm64 fest.
CAI_IMAGE=markusbayer109/cyberraum-cai@sha256:dd64e5c0aaf27308beb6e1bf570116c72cc42ce246fd521fee648d0f0af52ae5
JUICE_SHOP_IMAGE=bkimminich/juice-shop:v20.0.0

# Öffentliche Laufzeitkonfiguration; diese Datei direkt mit --env-file laden.
# Zugangsdaten, Anbieter, Basisadresse und Modell kommen aus Agent/.env.
# Agent/.env als zweite --env-file angeben, damit lokale Werte Vorrang haben.

# Workshoplimits
CAI_MAX_TURNS=20
CAI_MAX_INTERACTIONS=40
CAI_TOOL_TIMEOUT=90
CAI_PRICE_LIMIT=1
JUICE_SHOP_PORT=3000
